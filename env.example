# Configuración de Base de Datos SQL Server
# Si SQL Server está en la misma máquina que Docker, usa:
DB_SERVER=host.docker.internal\DATACENTERSERVER
# Si SQL Server está en otra máquina, usa su IP:
# DB_SERVER=192.168.1.100\DATACENTERSERVER
# Si es la instancia por defecto (sin nombre), quita \DATACENTERSERVER

DB_USER=sa
DB_PASSWORD=Administrador2025$$
DB_NAME=clientManager
DB_ENCRYPT=true
DB_TRUST_CERT=true
